# 專案進度紀錄

依照 `AI_投資顧問專案規劃書_v1.0.pdf` 逐步實作的 AI 投資顧問系統，本檔案記錄目前已完成的步驟，供後續版本開發參考。

## 已完成步驟

1. **讀取個人持股明細**：讀取 `個人持股明細.xlsx`，確認欄位為股票代號、名稱、持股股數、平均成本。
2. **建立 UV 專案管理**：以 `uv` 建立 `pyproject.toml` / `uv.lock`，加入 `pandas`、`openpyxl`、`yfinance`、`fastapi`、`uvicorn`、`httpx`、`requests` 等相依套件。
3. **依股票代號填入股票名稱**：撰寫 `fill_names.py`，依代號對照表將 17 檔持股名稱填回 Excel。
4. **讀取 AI 投資顧問專案規劃書並實作 v1.0**：
   - `portfolio.py`：核心邏輯，讀取持股、以 yfinance 抓取台股即時股價（自動嘗試 `.TW`／`.TWO`）與基本面（本益比、EPS、法人目標價、產業別、52 週高低點等）。
   - `main.py`：命令列進入點，產生報告並同步輸出 Markdown 與 Excel。
   - `app.py`：FastAPI 網頁儀表板（`uv run uvicorn app:app --reload`）。
5. **補充規格並擴充分析邏輯**：
   - 設定每月定期定額投入金額（實際金額為使用者個人資料，詳見 `portfolio.py` 的 `MONTHLY_INVESTMENT` 常數，不於本文件記錄數字）。
   - 新增合理買點估算（52 週區間 + 法人目標價）。
   - 新增市值佔比 vs. 成本佔比的再平衡檢查。
   - 新增產業配置建議（排除 ETF 分類，避免誤判集中度風險）。
   - 每月投資建議會排除已觸及停損門檻（虧損 ≤ -20%）的持股，避免自動建議往下攤平。
6. **建立固定格式投資報告（Markdown）**：`report.py` 產生 `投資報告.md`，涵蓋八大區塊（投資組合總覽、個股分析、合理買點、停利停損策略、產業配置建議、風險評估與再平衡、每月投資建議、後續追蹤重點），每次執行覆寫更新。
7. **將投資報告整理進 Excel**：`excel_report.py` 產生 `投資報告.xlsx`，以八個工作表對應相同的八大區塊，數值欄位套用百分比／千分位格式，方便在 Excel 中檢視與後續加工。
8. **整理專案進度紀錄**：本檔案 `專案進度.md`。
9. **v3.0 起步：凱基證券 API 串接（僅報價 + 庫存查詢）**：
   - 使用者已完成凱基 API 申請（風險預告書簽署、線上申請、憑證安裝）。
   - 安全性優先處理：`.gitignore` 加入 `.env`、憑證檔（`*.pfx`／`*.p12`／`*.pem`／`*.key`）、個人持股與投資報告檔，確保帳密與個人財務資料不會被上傳。
   - 發現相依套件相容性問題：`kgisuperpy` 只提供 Python 3.9–3.13 的預編譯執行檔，尚未支援本專案原本使用的 3.14，因此將專案 Python 版本鎖定為 **3.13**（`.python-version`、`requires-python = ">=3.13,<3.14"`），重新 `uv sync` 後確認 `kgisuperpy` 與既有的 `pandas`／`yfinance` 相容。
   - 新增 `python-dotenv` 依賴，帳密改由本機 `.env` 讀取（`.env.example` 提供範本，不含真實帳密）。
   - 新增 `kgi_client.py`：登入、查詢可用帳號、設定交易帳號、即時報價快照、庫存查詢。**依使用者指示，刻意不實作下單／委託功能**。
   - 新增 `test_kgi_connection.py`：供使用者在本機填好 `.env` 後自行執行連線測試（Claude 端沒有使用者的真實帳密，無法代為測試）。
   - 深入排查連線失敗原因：`KGICGCAPIATL2x64.dll` 與 `CGEnvDetectATL`（皆為 2014 年版本）都已確認註冊成功，但 `Login()` 呼叫時仍發生原生層級 access violation（`CheckCAInfo` 階段）。已排除 Python 版本（3.12/3.13 皆同樣失敗）、憑證有效性、防火牆／防毒等可能性。目前判斷是這組舊版元件與凱基現行後端協定不相容，**尚未解決，需凱基技術支援提供新版元件**。
10. **v4.0 起步：月營收、財報、重大訊息、新聞整合（測試通過並已上線）**：
    - 新增 `market_data.py`：全部使用免費公開資料源，不需申請、不需金鑰。
      - 月營收：台灣證交所 OpenAPI `t187ap05_L`
      - 財報重點（營收、EPS）：`t187ap06_L_*`（自動合併一般業／金控／證券期貨／保險／金融業，欄位名稱不同已正規化）
      - 重大訊息：`t187ap04_L`（上市公司每日重大訊息）
      - 新聞：Google 新聞 RSS（依股票名稱查詢，中文財經新聞，涵蓋 ETF／個股）
    - 已知限制：僅涵蓋上市公司；TPEx（上櫃）開放資料網站目前 SSL 憑證有問題連不上（非本專案程式問題）；法說會排程沒找到公開 API。
    - `report.py`／`excel_report.py`／`app.py` 都已新增對應的「九、市場情報」區塊／工作表。
    - 使用者確認：凱基庫存查詢維持手動更新 Excel（不等 API 修好）。
18. **v4.0 剩餘項目補完：法說會排程**：
    - 找到公開資訊觀測站（MOPS）「法人說明會一覽表」的實際查詢端點：`https://mopsov.twse.com.tw/mops/web/ajax_t100sb02_1`（POST，帶 `Referer` header 即可查詢，不需登入或金鑰）。原本 `mops.twse.com.tw` 網域＋這組參數會被 WAF 擋下（「因為安全性考量，您所執行的頁面無法呈現」），改打 `mopsov.twse.com.tw` 網域才成功。
    - 回傳為 HTML 表格（非 JSON／OpenAPI），用正規表示式解析 `<tr data-type='body'>` 每列的日期／時間／地點／擇要訊息；日期為民國格式（如 `115/07/16`，會議跨日則為「115/03/03 至 115/03/06」），已寫 `_parse_roc_date()` 轉換並取起始日供排序比較。
    - 查詢需指定市場別（`TYPEK=sii` 上市／`otc` 上櫃／`rotc` 興櫃），沒有「全部」選項；`portfolio.py` 的 `Holding` 新增 `market` 欄位，`fetch_market_data()` 依 yfinance 命中的 `.TW`／`.TWO` 後綴順便記錄是上市或上櫃，`market_data.py` 查法說會時優先用這個已知市場別（只需 1 次請求），未知時才依序嘗試上市／上櫃／興櫃。
    - 新增 `market_data.fetch_upcoming_investor_conference()`：回傳每檔持股「今天以後最近一場」法說會（日期／時間／地點）；`report.py`／`excel_report.py`／`app.py` 的「九、市場情報」都已加上對應欄位／文字（Excel 新增「下一場法說會」欄；網頁版已用 `html.escape()` 處理，避免 XSS）。
    - 已用使用者本機真實持股跑過 `main.py` 完整驗證：`投資報告.md`／`.xlsx` 都正確顯示某檔持股的法說會日期／時間／地點；ETF／無排定場次的個股正確顯示為空，不會噴錯。
    - **v4.0 至此全部完成**（月營收／財報／重大訊息／新聞／法說會排程五項皆已串接）。
11. **資安檢查（market_data.py 免費資料源）**：確認全專案只對外連線 `openapi.twse.com.tw`（台灣證交所官方網域）與 `news.google.com`（Google 官方網域）；程式碼搜尋 `eval`／`exec`／`pickle`／`subprocess`／`os.system`／`shell=True`／`verify=False` 皆無，無任意程式碼執行或停用憑證驗證風險；XML 解析用內建 `ElementTree`（預設停用外部實體展開，不怕 XXE）；新聞標題/連結顯示於網頁前已用 `html.escape()` 處理，避免 XSS。**結論：無資安或木馬疑慮**。
12. **v5.0 起步：Email 每日通知（選擇 Email，非 LINE）**：
    - 確認 **LINE Notify 已於 2025/03/31 終止服務**，官方替代方案 LINE Messaging API 需另外申請官方帳號＋開發者後台，設定較複雜，使用者選擇先做 Email。
    - 新增 `notify.py`：用 Python 內建 `smtplib`（無需新套件）透過 SMTP（預設 Gmail）寄信，帳密從 `.env` 讀取，支援附件。
    - 新增 `daily_report.py`：每日排程入口，產生報告後把摘要 + `投資報告.md`／`.xlsx` 當附件寄出，供 Windows工作排程器呼叫。
    - `.env.example` 新增 `EMAIL_ADDRESS`／`EMAIL_APP_PASSWORD`／`EMAIL_TO`／`EMAIL_SMTP_HOST`／`EMAIL_SMTP_PORT`。
    - **尚待使用者完成**：開啟 Google 帳戶兩步驟驗證、產生應用程式密碼、填入 `.env`，並執行 `uv run python notify.py` 測試寄信；排程仍需自行在 Windows工作排程器設定每日執行 `daily_report.py`。
13. **v5.0 完成：Email 測試成功 + 排程建立 + LINE 程式補上**：
    - 使用者完成應用程式密碼設定，`uv run python notify.py` 測試寄信成功；`EMAIL_TO` 改為使用者的公司信箱。
    - `uv run python daily_report.py` 完整流程測試成功（產生報告＋附件寄出）。
    - 建立 Windows 工作排程器任務「AI投資顧問_每日報告」，每天 **06:30** 執行 `daily_report.py`，**不需系統管理員權限**即可註冊成功；手動觸發驗證過排程環境下也能正常執行（`LastTaskResult = 0`）。
    - 使用者要求接續做 LINE：確認 **LINE Notify 已於 2025/03/31 終止**，改用官方 **LINE Messaging API**。為避免需要查詢個人 User ID（需架設 webhook 才能取得），改用 **Broadcast API**（廣播給所有好友），因為官方帳號設計為個人專用、好友只有自己，效果等同 Push 給自己，設定大幅簡化。
    - `notify.py` 新增 `send_line_broadcast()`；`daily_report.py` 會自動偵測 `.env` 有無 `LINE_CHANNEL_ACCESS_TOKEN`，有才發送 LINE、沒有就略過（不影響 Email）。
    - **尚待使用者完成**：於 LINE Official Account Manager 建官方帳號 → 該帳號啟用 Messaging API → LINE Developers Console 發行 Channel Access Token → 填入 `.env` → 用手機加官方帳號好友。
14. **v5.0 全部完成：LINE 設定成功，Email + LINE + 排程三者都驗證成功**：
    - 使用者建立官方帳號「AI投資顧問」（輕用量方案＝免費，每月500則訊息額度足夠使用）。
    - 過程中排查了兩次「找不到 Channel Access Token」的迷路：使用者先誤入 `developers.line.biz` 的公開文件教學頁（Documentation，不是自己的頻道管理畫面），後來才成功找到 `/console/` 登入後台；接著又在 OA Manager 完成「啟用 Messaging API」，Console 才出現對應 Provider/Channel。
    - 使用者在 Console 的 Messaging API 分頁發行 Channel Access Token（長期），貼給 Claude 後直接寫入 `.env` 的 `LINE_CHANNEL_ACCESS_TOKEN`。
    - `send_line_broadcast()` 測試成功，使用者手機確認收到訊息。
    - `uv run python daily_report.py` 完整跑一次，Email 與 LINE 同時成功發送。
    - **v5.0 至此全部完成**：每天 06:30 排程自動執行，同時寄 Email（含報告附件）與發送 LINE 通知。
15. **通知內容精簡（方案B雙軌分眾）**：Email 保留較完整摘要但排版更緊湊；LINE 只送一行極簡訊息。詳見上一則記憶更新，`daily_report.py` 的 `build_email_summary()`／`build_line_summary()`。
16. **家人加入 LINE 好友 + 分眾推播的技術限制**：太太掃 QR Code 加入好友後會自動收到 Broadcast。使用者想推播不同內容給她，但排查發現 `GET /v2/bot/followers/ids` 對免費方案回傳 403（功能未開放），手機號碼／個人分享連結都不是合法 User ID（實測 400 Bad Request）。提供三選項（架webhook／維持現狀／升級付費方案）後**使用者選擇暫時擱置**。
17. **家人 Email 加入又移除 + 全新「短線類股輪動觀察」功能**：
    - 一度把太太 Email 加進主報告的 `EMAIL_TO`，但使用者旋即要求「我的持股明細、金額只傳送給我」，移除該地址；同時要求 LINE 摘要也拿掉市值金額（已於上一則記憶更新處理）。
    - 太太真正想要的是**短線類股輪動交易建議**（3-7天進出），與本專案主軸（長期持股分析）完全不同，是全新獨立功能。
    - 新增 `sector_rotation.py`：`WATCHLIST` 人工整理 9 個台股常見熱門族群（記憶體/半導體/AI伺服器散熱/航運/金融/生技/網通/面板/綠能）各 3-5 檔代表股；依「3-7天平均漲跌幅」（ret_3d 與 ret_7d 取平均＝`momentum`）排名族群與族群內個股；買入區間＝5日/10日均線；獲利了結區間＝近10日高點；預估天數＝用近3日日均漲幅回推、限制在2~10天。**這是規則型技術面參考，非價格預測**，已於報告內明確加註風險提醒。
    - 新增 `spouse_report.py`：把輪動觀察獨立寄給太太，**BCC（密件副本）一份給使用者自己**（`notify.send_email()` 已加上 `bcc` 參數；Python smtplib 的 `send_message()` 會自動從送出的訊息移除 Bcc 表頭，收件人看不到副本对象）。
    - 內容格式反覆調整多次（使用者要求橫向排版、簡短、明確欄位標籤），最終定案：`■ 輪動排名N：族群名` + 每檔個股一行「名稱（代號)　買入區間：X~Y　獲利了結區間：約Z　預估天數：約N天」。
    - 建立第二個 Windows 排程任務「AI投資顧問_太太短線報告」，同樣每天 **06:30** 執行 `spouse_report.py`，跟主報告任務各自獨立、互不影響；手動觸發驗證過排程環境下正常執行（`LastTaskResult = 0`）。
18. **凱基 API 未修好之前，額外補上三項免費公開資料源**（使用者問「除了個人庫存，還有哪些資料無法取得」後決定新增）：
    - **大盤（加權指數）**：`market_data.fetch_market_index()`，用證交所 OpenAPI `exchangeReport/MI_INDEX`；報告「一、投資組合總覽」新增大盤收盤與漲跌點數／百分比，供對照個股與大盤的相對表現。
    - **更即時股價**：`market_data.fetch_realtime_quotes()`，改打證交所官方看盤 API `mis.twse.com.tw/stock/api/getStockInfo.jsp`（免金鑰，批次查詢），比原本 yfinance 的延遲快照更即時；`portfolio.enrich_with_market_data()` 會用這個結果覆蓋 `holding.price`，抓不到才維持 yfinance 的價格。
    - **三大法人買賣超**：`market_data.fetch_institutional_flow_map()`。上市（TWSE）用官方 `www.twse.com.tw/rwd/zh/fund/T86`，欄位有清楚標示可拆分外資／投信／自營商細項；上櫃（TPEx）用 `www.tpex.org.tw/www/zh-tw/insti/dailyTrade`，但這個 JSON 欄位重複無法明確拆分細項，**只取三大法人合計欄**，避免誤植成錯的分類。當天資料抓不到（例如非交易日）會自動往前找最多 5 天。
    - 三者都已加進 `report.py`／`excel_report.py`／`app.py`；已用使用者本機真實持股完整跑過 `main.py` 驗證：大盤指數、即時報價、三大法人買賣超（含外資／投信／自營商細項）都正確顯示，無資料的欄位（如 ETF 沒有三大法人細項）正確顯示為空，不會噴錯。
19. **建立 6 個 Claude Code 專案技能 + Email 附件改為 PDF（大字體）**：
    - 在 `.claude/skills/` 建立 6 個專案技能（`investment-report`／`daily-report`／`spouse-report`／`sector-rotation-preview`／`kgi-connection-test`／`investment-dashboard`），依複雜度分配模型（機械式操作 `haiku`、需摘要判斷 `sonnet`、需診斷推理的凱基連線測試 `opus`），觸發詞與底層動作整理在 `技能清單.docx`。
    - 為了讓「類股輪動預覽」能獨立於寄信之外使用，把 `spouse_report.py` 裡的 `build_report_text()`／`DISCLAIMER` 搬到 `sector_rotation.py`，並加上 `__main__`，`spouse_report.py` 改為引用它（行為不變，已重新測試）。
    - 使用者要求「每日報告 Email 附件不要寄 Markdown 檔，改用 PDF，而且整體字體要 14pt 以上（老花眼）」。**關鍵技術陷阱**：一開始用 `xhtml2pdf`（HTML→PDF）搭配 Windows 內建標楷體（`kaiu.ttf`）產生 PDF，即使正確用 `pdfmetrics.registerFont`／`registerFontFamily` 註冊字型，中文字元還是整段跑版成缺字方框（■），連文字擷取都是亂碼，換用 `@font-face` CSS 也一樣失敗（xhtml2pdf 對本機字型路徑的 URL 處理有 bug）。**改用純 `reportlab`（platypus flowables，不透過 HTML）直接繪製才正常**，同一顆 `kaiu.ttf` 字型完全正確渲染。
    - 新增 `pdf_report.py`：用 reportlab 重現 `report.py` 的九大區塊（含大盤指數、三大法人買賣超等 v4.0 新資料），內文／條列字體固定 14pt，表格因欄位較多折衷用 12pt，橫向 A4 排版。已加為正式相依套件（`uv add reportlab`），非暫時性腳本。
    - **順手修掉效能問題**：`daily_report.py` 原本每次執行會呼叫 3 次 `build_report_context()`（`generate_investment_report`／`generate_excel_report`／email 摘要各自重算一次，market_intel 那些外部 API 呼叫等於白白多打 2 次），藉這次加 PDF 的機會幫 `generate_investment_report()`／`generate_excel_report()` 都加上可選的 `ctx` 參數，`daily_report.py` 現在只算一次 `ctx` 重複利用給三種輸出格式＋Email 摘要。
    - `daily_report.py` 寄送附件從 `[REPORT_PATH, EXCEL_REPORT_PATH]`（.md + .xlsx）改為 `[PDF_REPORT_PATH, EXCEL_REPORT_PATH]`（.pdf + .xlsx）；本機仍會照常產生 `投資報告.md`（給 `/investment-report` 技能與其他本機用途），只是不再附加在信件裡。`投資報告.pdf` 已加入 `.gitignore`（跟 .md/.xlsx 一樣是個人財務資料）。
    - **實際觸發驗證時發現的小 bug**：`daily_report.py` 的 LINE 摘要含 📊 emoji，Windows 主控台非 UTF-8 編碼（cp950）下 `print()` 會 `UnicodeEncodeError` 崩潰——但崩潰點在 `send_email()`／`send_line_broadcast()` 都已執行完成之後，代表**通知其實已經寄送成功，只是收尾的 print 掛掉**。已在檔案開頭加 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` 修正，之後不論在什麼主控台編碼下執行都不會再有這個問題。
20. **停利停損規則加入「法人／市場是否持續看好」的判斷**：
    - 使用者新規則：獲利門檻從 +30% 提高到 **+50%** 才建議分批停利；虧損門檻維持 **-20%** 觸發「檢視基本面評估停損或加碼」。**但**不論獲利或虧損觸及門檻，都會先看法人是否持續看好（`portfolio.is_market_bullish()`：法人評等為 `buy`／`strong_buy`，或法人目標價比現價高 15% 以上），看好的話一律建議**續抱**，不盲目停利出清或停損。
    - 新增 **-30% 硬門檻**（`STOP_LOSS_HARD_THRESHOLD`）：虧損擴大到這裡，不論法人看法如何都直接建議停損（使用者原話舉例：像某檔生技股一路破底的情況）。已用真實持股資料驗證：某檔獲利部位（strong_buy、法人目標價上看一定幅度）→「建議續抱，暫不停利出清」；兩檔虧損部位（分別超過-30%與-70%門檻）→ 都正確觸發「不論法人看法，建議停損」。
    - `report.py`／`excel_report.py`／`pdf_report.py` 的「統一原則」說明文字都同步更新，三者一致。ETF（如 0050、00918）因 yfinance 通常沒有 `recommendationKey`／目標價資料，`is_market_bullish()` 一律回傳 `False`，維持原本單純看損益率門檻的行為，不受影響。
    - `monthly_allocation_suggestion()`（每月定期定額排除已停損標的的邏輯）**維持不變**，仍以 -20% 為排除新資金加碼的門檻，不受這次法人看好與否的判斷影響——即使法人看好，已下跌 20% 以上的部位本月新資金也不會自動往下攤平，這是刻意保留的既有保守原則。
    - **後續調整**：`is_market_bullish()` 原本法人目標價要「高於現價 15% 以上」才算看好，使用者用自己一檔獲利部位舉例後改為單純「法人目標價 > 現價」即算看好（移除 15% 緩衝），並且觸發續抱建議時**訊息會附上實際的法人目標價數字**，不再只說「持續看好」四個字。範例：「某檔持股：獲利XX%，但法人／市場持續看好，法人目標價OOO元，建議續抱，暫不停利出清。」
21. **AI 綜合評分與行動建議系統（六構面加權評分 + 四色訊號）**：使用者提出一套完整的規格（見對話紀錄），核心精神「AI 的目標不是預測股價，而是回答該不該採取行動，且要結合股票本身＋使用者持股狀況」。走過完整 Plan Mode 流程（先用真實 API 驗證資料可行性，再讓使用者確認範圍才動手），使用者確認：**一次做完整系統**（不分階段）；DCF／FOMC／CPI／台指夜盤等無免費資料源的項目**跳過**，改用既有新聞機制帶過；可投入現金／風險承受能力／投資期限**比照 `MONTHLY_INVESTMENT` 寫死在設定裡**。
    - **資料可行性研究成果**（實測過，非猜測）：技術指標（月季年線/RSI/KD/MACD/布林/ATR/支撐壓力/VWAP）用 yfinance 歷史股價自算；市場環境（VIX/SOX/NASDAQ/美元指數/台幣/TSM ADR/NVDA/AMD/INTC）yfinance 全部可用；融資融券 `exchangeReport/MI_MARGN`；董監持股 `t187ap11_L`、大股東 `t187ap02_L`（比原本設想的 TDCC API 更直接，同一套證交所 OpenAPI 目錄下就有）；財務比率用 `t187ap17_L`（毛利率/營業利益率/純益率）＋ `t187ap07_L_*`（資產負債表，換算負債比/流動比/每股淨值/概算ROE/ROA）＋ `t187ap45_L`（股利分派）；`t187ap06_L_ci` 損益表本來就有「本期淨利（淨損）」欄位可直接抓淨利算 ROE/ROA，不用額外估算。
    - **新增模組**：`technicals.py`（技術面指標）、`market_environment.py`（市場環境）、`scoring.py`（核心評分引擎：六構面加權 Investment Score(0-100) + 獨立 Risk Score(0-100) + 四色訊號決策 `decide_signal()` + 狀態記憶節流 `alert_state.json`）；`market_data.py` 擴充融資融券/董監持股/大股東/資產負債表/營益分析/股利分派共 6 個新函式。
    - **兩個關鍵除錯過程，值得記住**：
      1. **「有分數」≠「資料充足」**：一開始用「六個類別是否都有分數」當信心指標，結果 ETF 光靠殖利率+今日成交量就能讓每個類別「有分數」，顯示 100% 覆蓋率，但基本面/成長性/籌碼面其實幾乎全是缺資料，訊號還是很有自信地給「建議賣出」。改成用每個類別「內部子項目」的實際覆蓋率加權平均，ETF 才正確顯示 38-45% 低覆蓋率並在標籤加註「資料覆蓋率低，訊號僅供參考」。
      2. **PDF 合理價 fallback 誤判追高**：一開始 `estimate_fair_value()` 在沒有法人目標價時，退而求其次用「52週區間中位數」概算合理價，結果讓 0050、00981A 等 ETF 全部被誤判成「❌不建議追價」——因為這類 ETF/正2 商品的 52 週低點常被單一次系統性下跌拖低，中位數嚴重低估合理價。改成**沒有法人目標價就直接回傳 None、不猜**，寧可缺資料也不要用不可靠的估算誤導使用者。
    - **驗證通過的真實案例**：某檔虧損超過-70%極端門檻的持股 →「已知巨額虧損部位，除非重大變化不再頻繁提醒」（節流生效，不再每天喊建議停損）；某檔獲利部位（法人持續看好）→「獲利豐厚但法人／市場持續看好，建議續抱，不建議賣」；某檔虧損超過-30%硬門檻的持股 →「虧損擴大，不論法人看法，建議停損」（不受節流影響，因為還沒到-70%極端虧損分支）。
    - `report.py`／`excel_report.py`／`pdf_report.py`／`app.py` 的「四、停利停損策略」都改成「四、AI 綜合評分與行動建議」，顯示訊號＋Investment Score＋Risk Score＋法人目標價＋關鍵理由。`daily_report.py` 的 Email/LINE 摘要套用節流：極端虧損股在安靜期內不會出現在通知摘要（完整報告仍會列出），且過濾掉資料覆蓋率 <50% 的低信心訊號（避免 ETF 雜訊淹沒真正重要的提醒），LINE 摘要從舊的「N檔停損觀察」改成「N檔AI提醒」。
    - **PDF 專屬踩坑**：`scoring.py` 的訊號用 🟢🟡🟠🔴 emoji，但 PDF 用的標楷體（KaiU）字型沒有 emoji 字符集，reportlab 遇到不支援的字符是**直接跳過、不是顯示缺字方框**，很容易沒發現（連原本 `_market_intel_flowables` 用的 "‣" 項目符號也是同樣狀況，已一併修正）。改成 `pdf_report.py` 專屬的 `_pdf_safe()`／`_signal_marker()`，把 emoji 轉成有顏色的中文方括號標籤（例如 🔴 → 紅色「【全數賣出】」），Markdown／Excel／網頁版則維持原本的 emoji（那些平台都能正常顯示，不受影響）。
    - 個人化設定新增在 `portfolio.py`：`AVAILABLE_CASH`／`RISK_TOLERANCE`／`INVESTMENT_HORIZON`，比照 `MONTHLY_INVESTMENT` 寫死，要調整直接改常數。`alert_state.json` 已加入 `.gitignore`（個人使用記錄，不上傳）。
    - **How to apply**：以後若使用者要調整評分權重、門檻、關鍵字清單，直接在 `scoring.py` 對應常數改（`POSITIVE_CATALYST_KEYWORDS`／`NEGATIVE_CATALYST_KEYWORDS`／各 `score_*()` 函式裡的映射區間）；若要新增新的評分子項目，記得同時處理「缺資料時如何優雅降級」，不要為了讓分數看起來完整就硬湊數字或用不可靠的估算頂替，這是這次除錯學到最重要的原則。
22. **薪資現金流投資人系統（現金流優先、分批買進 Level 1-4、優先購買清單、Conviction Score）**：使用者定義自己是「薪資現金流投資人」——每月固定投入設定金額、不借貸不融資、長期持有5-20年、滾雪球累積，因此要求 AI 不能提出超出可執行能力的建議，且「如果本月資金只夠買，就只買第一順位，不是每檔都買一點」。
    - **釐清三個關鍵設計問題**（都先問過使用者才動手，避免猜錯方向）：(1) 帳上現金／股利／已實現獲利怎麼提供 → **使用者選擇在 `個人持股明細.xlsx` 新增「現金流」分頁**（日期/項目/金額/備註，金額正負號自己代表存入或扣除，已加入範例列＋使用說明），已實際新增到使用者的真實 Excel 檔案（原本的 Sheet1 持股資料完全沒動）。(2) 未投入資金結轉上限 → **最多累積 3 個月投入金額**（`MAX_CARRYOVER_MONTHS`）。(3) Conviction Score（長期持股信心分數）沒辦法量化「競爭優勢/管理能力」→ **用量化代理指標組成並誠實標註限制**（ROE、毛利率、負債比、市值規模、是否穩定配息），不是真正的護城河評估。
    - **新增 `portfolio.load_cash_flow_balance()`**：讀「現金流」分頁加總金額欄，分頁不存在時優雅回傳 0（不噴錯，向下相容舊版 Excel）。實測：範例列（股利+1500、已投入-3000）正確加總成 -1500。
    - **新增 `scoring.score_conviction()` + `conviction_stars()`**：0-100 分轉 1-5 星（`★★★★★`）。實測結果跟使用者自己舉的範例表**驚人地吻合**：三檔實測持股的量化分數與星等，跟使用者憑主觀認知舉的範例分數／星等幾乎一致（體質較佳的兩檔都落在4-5星、體質較差的一檔落在1星），驗證量化代理指標的方向是對的。星星符號 ★／☆ 有另外測試過可以在 PDF 標楷體正常顯示（不像 emoji 會消失）。
    - **重寫 `portfolio.monthly_allocation_suggestion()`**：加入優先購買清單（依 Investment Score／Conviction Score 排序）、分批買進 4 個 Level（市場正常20% / 跌到合理價30% / 最佳買點30% / 恐慌大跌20%，依現價相對法人目標價與52週區間判斷）、等待成本計算（比較法人預估上漲空間 vs 目前高估幅度，決定該提前布局還是等待拉回，對應使用者舉的範例：小幅溢價但預估報酬仍高就不要空等）、持股配置上限（個股25%／ETF 30%，取代原本統一的30%集中度門檻）。
    - **實測抓到的重大邏輯 bug**：第一版讓資金依比例「一直往下一順位分配」，結果本月總預算 cascade 分給了全部 15 檔持股，最小一筆只有個位數金額——完全違反使用者明確講的「不是每檔都買一點」。**修正**：改成每個優先順位的投入比例都以「本月總預算」為基準（不是剩餘金額的百分比），並加上 `MAX_MONTHLY_FUNDED_CANDIDATES = 3` 上限與「低於預算5%就不再往下分配」的門檻，讓資金確實集中在前面幾檔。修正後：前三順位各拿約三成預算、其餘結轉下月，符合使用者要求的「集中投入」精神。
    - **連帶抓到並修好的 render bug**：`monthly_allocation_suggestion()` 回傳格式從「一段文字」變成「多行結構化清單」後，`report.py`／`pdf_report.py`／`app.py` 原本會自動幫每一行加項目符號（`- ` 或 `<li>` 或 PDF 的 "・"），套在已經自帶編號/標題/空行的新內容上，變成一堆空的「- 」和被重複加符號的編號列。三個檔案都改成直接輸出多行內容，不再自動加符號（`excel_report.py` 本來就是一行一格不受影響）。
    - **AI_Agent_架構規格_v2.0.md 併入 CLAUDE.md**：使用者提供一份理想化的 21-Agent Multi-Agent 架構文件（Data Collector／Financial Analyst／...／Investment Chief AI），確認過**只當架構參考與角色對照表，不拆成 21 個真的獨立呼叫的 LLM subagent**（原因：這些工作本質上是確定性計算，Python 公式比逐一呼叫 LLM 快、便宜、穩定），已整理成「v2.0角色 → 現有實作」對照表寫進 CLAUDE.md。**該文件的權重表（財報25%/估值20%/...）跟現有 scoring.py 的六構面權重是不同分類系統，不要混用或誤以為要對齊**，已在 CLAUDE.md 特別註記。
    - **太太的短線輪動觀察也加嚴**：`sector_rotation.py` 新增 RSI14/成交量比對（重複利用 `technicals.compute_technicals()`，同一份歷史股價不用多打一次 API），RSI14 > 80（嚴重過熱）直接排除、依動能排序往下找下一檔，RSI 70-80 或量能不足則列入但附加「⚠」提醒，不是機械式取前 N 檔。實測有抓到「量能不足以確認動能」的真實案例（長榮、智邦）。
    - **又一次踩到 emoji console print 的坑**：`sector_rotation.py`／`spouse_report.py` 新加的「⚠」符號在 Windows cp950 主控台一樣會讓 `print()` 崩潰，已比照先前 `main.py`／`daily_report.py` 的做法補上 `sys.stdout.reconfigure()`。**這已經是第三次踩到同一類坑了**：只要輸出內容可能包含非 Big5/CJK 字符集的符號（emoji、⚠、‣ 等），任何新的 `print()` 到終端機的地方都要考慮這個問題，PDF 那邊則是完全不同的坑（KaiU 字型不支援導致字符直接消失，不是印不出來）。
    - LINE 官方帳號更名為「豬家股市紅飆(AI)」（原「AI投資顧問」），已同步更新 README.md。
    - **How to apply**：使用者的持股/現金流資料現在分散在 `個人持股明細.xlsx` 的兩個分頁，讀取邏輯要同時處理兩邊；若使用者要調整分批買進的 Level 比例或持股上限，直接改 `portfolio.py` 開頭的常數（`BUY_LEVEL_*_PCT`／`INDIVIDUAL_STOCK_MAX_WEIGHT`／`ETF_MAX_WEIGHT`／`MAX_MONTHLY_FUNDED_CANDIDATES`）。
23. **實際觸發全部通知並排查一次公司信箱收不到信的狀況**：`daily_report.py`／`spouse_report.py` 都用使用者真實資料實際寄送過，LINE 秒收到，但公司信箱（`yonyu.com.tw`）當下沒立刻收到 Email，一度懷疑是 bug。
    - **除錯過程**：檢查 `notify.py` 發現附件一律標示成通用的 `application/octet-stream`（不管實際是 PDF 還是 Excel），這對企業郵件過濾器來說是常見的可疑訊號，於是先做了防禦性修正——改用 Python 內建 `mimetypes.guess_type()` 依副檔名給正確的 MIME type（`.pdf`→`application/pdf`、`.xlsx`→`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`），並重新寄送驗證。
    - **真正原因**：使用者事後回報收信匣的信件時間戳，比對後發現**修正前寄出的原始那封信本來就有到，只是比寄送時間晚了幾分鐘**（公司信箱對外部帶附件信件的掃描/過濾佇列造成的正常延遲），不是 MIME type 或程式邏輯的問題。MIME type 修正本身仍是合理的防禦性改善予以保留，但**這次的延遲不是它解決的**，記錄下來避免以後誤判成「MIME type 修正修好了」。
    - **使用者決定**：與其每次等公司信箱延遲，不如把兩個排程都提早——**`daily_report.py`／`spouse_report.py` 的 Windows 工作排程器觸發時間從 06:30 改成 05:00**（`Set-ScheduledTask` 更新 Trigger，Action/WorkingDirectory 都沒變動，已用 `Get-ScheduledTask` 驗證過）。README.md 已同步更新時間。
    - **How to apply**：以後使用者再回報「沒收到信」，先確認是不是公司信箱單純延遲幾分鐘（可請使用者提供實際收信時間戳比對寄送時間），不要一開始就假設是程式 bug；`notify.py` 的附件 MIME type 已經是正確做法，之後新增附件類型不需要再改。
24. **盤中開盤強弱觀察（Market Microstructure Agent，僅限太太的短線輪動）**：使用者丟了一份對「開盤溢價率」判斷強弱規則的深度自我審查（原始三條規則的優缺點、可信度評分、修正版 Rule 1-3、最後提議一個「Open Premium Agent」＋「Market Microstructure Agent」）。
    - **先做資料可行性查證，沒有直接動手**：實測 `mis.twse.com.tw` 看盤 API 除了已知的開盤價/昨收欄位，還有 `f`（委賣量）／`g`（委買量）欄位，跟五檔買賣「價」的 `a`／`b` 配對（查證過社群文件確認欄位語意，非自己猜測），代表委買委賣比也拿得到；另外實測 yfinance 對台股支援盤中分鐘級資料（`interval="5m"`／`"30m"` 都測過有真實資料），30分鐘K棒可行。
    - **點出使用者自己問題裡的架構矛盾**：使用者問「何時要再寄送提醒」，這揭露了一個根本問題——開盤溢價率、30分鐘K棒這些東西在現有的 05:00 排程（開盤前）根本不存在，一定要盤中觸發才有意義，跟專案其他「一天跑一次」的模組完全不同模式。也順帶點出這套規則本質上是短線timing工具，跟使用者稍早明確定義自己是「薪資現金流投資人」（長期持有、不追高頻）方向上有落差。用 AskUserQuestion 問清楚兩件事都得到明確答案：(1) 只用在太太的短線輪動觀察，不影響使用者本人的月度配置決策；(2) 盤中排程訂在 09:35（開盤30分鐘確認）與 11:30（盤中觀察），各寄一次。
    - **新增 `microstructure.py`**：`fetch_snapshot()`（開盤/昨收/今高今低/現價/累積成交量，市場別未知時自動嘗試上市/上櫃）、`fetch_opening_30min_low()`（yfinance 5分K，`between_time("09:00","09:30")` 取開盤半小時最低價）、`fetch_avg_daily_volume()`（近20日均量當量能比對基準）、`score_open_premium()`（依使用者修正後的三條規則換算 0-100 分：強勢確認+15、開高走低-20、爆量價漲+10、爆量黑K-25）。**沒有做**集合競價強度（沒找到免費資料源）與真正逐筆 VWAP（用盤中K棒近似即可，不需要逐筆）。
    - **新增 `spouse_intraday_check.py`**：重跑 `sector_rotation.scan_sector_rotation()` 取得當天候選股（日線動能排名，盤中重跑理論上跟早上一致，不需要額外狀態同步），對每檔算開盤強弱分數，組成簡短更新寄給太太（BCC 使用者），跟 05:00 的完整輪動報告是獨立排程。
    - **新增兩個 Windows 排程任務**：「AI投資顧問_太太盤中觀察_0935」「AI投資顧問_太太盤中觀察_1130」，比照既有任務的 Principal 設定（`LogonType Interactive`／`RunLevel Limited`，不需要系統管理員權限）。已用 `Start-ScheduledTask` 手動觸發驗證過排程環境下能正常執行（`LastTaskResult = 0`），且是真實寄出（不是模擬）。
    - **使用者立刻抓到的疏漏**：第一版 `spouse_intraday_check.py` 沒有檢查「現在到底是不是交易時段」，每天 09:35／11:30 都會無條件寄送，週末／國定假日也會寄出用舊資料湊出來的失真報告。**修正**：`microstructure.is_trading_session_now()` 三層判斷——週一~週五、09:00~13:30、且 MIS API 回傳的即時報價日期欄位（`d`）真的等於今天。第三層最關鍵：國定假日 MIS API 只會回傳最後一個交易日的舊資料，日期會兜不上今天，靠這個順便排除國定假日，不用額外維護一份台股假日表。非交易時段會直接印出訊息並 return，完全不寄送、也不會浪費 API 呼叫去抓候選股清單。
    - **How to apply**：這套盤中評分是規則型的粗略觀察因子，**沒有統計驗證**，使用者自己也明確講過（「連番漲停沒有任何學術研究支持」）——以後如果要調整規則的加減分數值或門檻，直接改 `microstructure.py` 開頭的常數（`STRONG_OPEN_PREMIUM`／`HOLD_ABOVE_PCT`／`VOLUME_CONFIRM_RATIO`／`BLOWOUT_VOLUME_RATIO`），不要因為看起來準就當成必勝規則升級成使用者本人長期持股的決策依據，那條界線是使用者自己劃的。以後任何盤中觸發的新功能，都要記得先檢查 `is_trading_session_now()`，不要預設排程時間到了就一定要寄。
25. **太太的短線輪動觀察加一個「個股動能 Top5」清單**：使用者要求「強勢輪動族群的前5名，個股前5名，以股價單價高低排序(由低到高)」。
    - `sector_rotation.py`：族群排名顯示範圍從前 3 名擴大到前 5 名（`build_recommendations()` 的 `top_n_groups` 預設 3→5）；新增 `build_top_stocks_by_price()`，不分族群、跨全 `WATCHLIST` 依 3-7天動能取前 5 檔個股（一樣排除 RSI14 過熱的追高風險股），再依**股價單價由低到高**重新排序，方便太太快速比較好入手的標的。抽出共用的 `_build_stock_entry()` 避免兩處重複組字典的邏輯。
    - `spouse_report.py`：`main()` 呼叫新的 `build_top_stocks_by_price()`，把結果傳進 `build_report_text()` 的新參數 `top_stocks_by_price`，在報告最後多印一段「■ 個股動能 Top5」區塊，每檔附註所屬族群。
    - `spouse_intraday_check.py` 沒有改動——族群前 5 名的擴大範圍會間接影響它（`build_recommendations()` 預設值共用），盤中候選股從最多 6 檔變成最多 10 檔，但「個股動能 Top5（依股價排序）」這個新清單只加在 05:00 的完整輪動報告，盤中簡短更新不需要。
    - 用真實資料跑過 `sector_rotation.py` 驗證：族群排名正確顯示到第 5 名，Top5 個股清單依股價由低到高排序（萬海87→和碩88.4→聯詠507→奇鋐2380→緯穎5730），且每檔正確標出所屬族群。
    - **順帶發現（未處理）**：`WATCHLIST` 裡有 6 檔（3260 威剛／8299 群聯／4174 浩鼎／6547 高端疫苗／6244 茂迪／3691 碩禾）目前 yfinance 抓不到資料（404 possibly delisted），需要之後找時間確認代號是否變更或已下市，不影響本次功能，先記錄下來。
    - **How to apply**：以後若要調整「個股動能 Top5」的檔數或改成別的排序基準，改 `build_top_stocks_by_price(scan_result, top_n=5)` 的參數與內部 `top.sort(key=lambda m: m.price)` 那行即可，不用動 `build_recommendations()`。
    - **緊接著的調整**：使用者確認後把族群內選股也擴大——`build_recommendations()` 的 `top_n_stocks` 預設從 2→5（族群 watchlist 代表股不足 5 檔時就顯示全部，例如「網通」只有 2 檔就顯示 2 檔）。用真實資料驗證過：AI伺服器/散熱族群從原本 2 檔擴大顯示到 4 檔（技嘉、廣達也列進來了）。
    - **新增「重電」「軍工」兩個族群**：先用 WebSearch 查證真實代表股與代號（不是憑印象猜），避免代號錯誤或抓錯公司。重電（`1513`中興電／`1519`華城／`1503`士電／`1504`東元／`1514`亞力）、軍工（`2634`漢翔／`8033`雷虎／`6753`龍德造船／`5371`中光電，其中 `5371` 是上櫃股，`.TW` 會 404 後自動 fallback 到 `.TWO` 才抓到，屬於既有正常機制）。加入後 `WATCHLIST` 共 11 組族群。用真實資料驗證過兩個族群都能正確算出動能（今天重電 -2.33% 排名6、軍工 -12.12% 排名11），只是動能不夠強所以沒進前5名顯示，機制正確、之後動能轉強會自然出現。
    - **太太的 Email 改成 HTML 版、股票名稱/代號上色＋字體加大**：`notify.send_email()` 新增 `html_body` 參數，用 `EmailMessage.add_alternative()` 送 multipart/alternative（純文字仍是備援，收信端不支援 HTML 時顯示）。`sector_rotation.py` 新增 `build_report_html()`、`spouse_intraday_check.py` 新增 `build_intraday_html()`（並把原本內嵌在 `build_intraday_text()` 裡的 `microstructure.evaluate()` 呼叫抽成 `_collect_intraday_results()`，文字版/HTML版共用同一份結果，不重打兩次 API），股票名稱用玫瑰紅（`#c2255c`）、代號用寶藍（`#1864ab`），都是粗體 16px；一般內文 15px、標題 17px、免責聲明最小 14.5px，符合使用者要求的「14.5~16px 以上」與「顏色要醒目」。兩封信（05:00 完整報告／盤中觀察）共用同一套配色常數，視覺一致。實際寄送驗證過 HTML 內容正確產生（`<span>` 顏色標籤、字級都在預期範圍）。
    - **How to apply**：以後若要調整太太 Email 的顏色或字級，改 `sector_rotation.py`／`spouse_intraday_check.py` 開頭的 `HTML_*` 常數即可（兩個檔案目前是各自獨立的常數，沒有共用模組，故意保持簡單不做過度抽象）。
    - **緊接著加碼**：使用者看了實寄的 Email 截圖後，額外要求「獲利了結區間的價位也要上色變粗體」，補上 `HTML_SELL_ZONE_COLOR = "#0ca678"`（翠綠），套用在 `_format_stock_html()` 的 `約{sell_zone}` 那段。
26. **短線輪動預測的事後驗證追蹤＋每週五彙總（`rotation_tracking.py`，新增，同一輪對話內經過三次追加需求）**：使用者看到「緯穎預估天數約2天、潛在損益率約+10.4%」後，要求「記憶上述、2天後驗證，再把差異回歸程式修正」，接著陸續補了三個需求：(1) 把落差跟推斷原因記錄起來、每3~5天整理寄給使用者本人自己判斷要不要調整規則（不要自動改程式）；(2) 明確改成「每週五」寄；(3) 「務必每個誤差都要查出誤差原因」，不接受「原因不明」。
    - **範圍界定不變**：只做到「記錄＋驗證＋推斷原因＋定期攤給使用者看」，**沒有自動修改公式的機制**——落差成因需要人判斷，累積到有規律的真實案例才由使用者決定要不要調整 `sector_rotation.py`。
    - **`log_predictions()`**：把 `build_recommendations()`／`build_top_stocks_by_price()` 的結果（股價、預估報酬率、預估天數、當時的 `caution_note` 警訊）寫進 `rotation_predictions.json`，`due_date = 預測日期 + estimated_days`（日曆天數概算）。
    - **`verify_due_predictions()`**：到期的預測重新抓實際股價算出 `diff_pct`，呼叫 `_infer_diff_reason()` 推斷原因。
    - **`_infer_diff_reason()` 的「不接受原因不明」設計**：依序檢查「預測當下已有警訊」「同期加權指數（`^TWII`）同方向同幅度與否，判斷是不是大盤系統性因素」「新聞標題關鍵字偏多偏空（`market_data.fetch_news()`＋自訂關鍵字，跟 `scoring.py` 的清單分開維護，避免把太太的短線系統跟使用者長期持股的評分系統耦合在一起）」「驗證當下的 RSI14／量能比是否轉弱」，能查到幾項就列幾項、用分號串起來；真的什麼訊號都沒有時，**不會回傳「原因不明」**，而是給一個具體結論：「規則本身對這檔股票的預測力可能不足，建議留意個股特性」，把最後一種情況也轉成可行動的診斷而不是死路。
    - **每週五彙總，不混進太太的信**：驗證結果完全不會出現在太太每天收到的輪動報告裡；`maybe_send_summary(send_fn)` 只在「今天是週五」且「有已驗證但還沒彙總過的紀錄」時才觸發，呼叫端注入的 `send_fn` 負責實際寄信（`spouse_report.py` 裡寄給 `notify.EMAIL_TO`，也就是使用者自己，不寄給太太）。用 `_meta.last_summary_date` 的 ISO 年週比對，避免同一週五重複手動執行時又寄一次。
    - **配色避開綠色**：使用者事後特別提醒「股市綠色代表跌價，請避免」——原本落差為正（比預估好）用綠色的設計是誤用，已全面改成：落差優於預估用紅（`#e03131`，符合台股紅漲綠跌的直覺）、劣於預估沿用既有警示橘（`#e8590c`），彙總信跟太太的日報一致不使用綠色。獲利了結區間的價位也一併從翠綠改成紅色並加粗到 `font-weight:900`（使用者額外要求「字體再加粗」）。
    - **驗證方式**：用真實股價資料構造一筆「5個交易日前預測、今天到期」的模擬紀錄（不是同一天湊出的假資料，避免大盤比對區間長度為0），跑 `verify_due_predictions()` 確認能產出多因子組合的推斷原因（實測台積電案例：「同期大盤同步走揚＋RSI轉弱＋量能萎縮」三項並列）；用 `unittest.mock.patch` 模擬「今天是週五」「今天是週六」「同一週五重複執行」三種情境，驗證 `maybe_send_summary()` 只在週五且有未彙總紀錄時觸發、且不會同週重複寄送。測試資料驗證完即刪除，改用真實流程重新產生。
    - **`rotation_predictions.json` 已加入 `.gitignore`**（比照 `alert_state.json`，屬個人使用記錄，不上傳）。
    - **順帶發現、當下未處理，下一則已修正**：`app.py`（使用者自己的長期持股網頁儀表板）裡也有「綠色代表獲利、紅色代表虧損」的同款配色反邏輯，使用者這次只針對太太的短線信件反映，先記錄下來——使用者緊接著在下一則訊息確認「要」修，見第 27 則。
    - **How to apply**：`_infer_diff_reason()` 的檢查順序就是優先權順序，之後如果要新增新的歸因因子（例如同族群其他股票的當期平均落差，判斷是不是整個族群系統性跑掉），直接在 `market_ret` 檢查後面插入新的 `reasons.append(...)` 區塊即可，維持「查到幾項列幾項、查不到就給規則預測力不足的結論」這個不留死路的原則。彙總寄送週期若要改回「3~5天區間」而非固定週五，改 `_due_for_summary()` 的判斷邏輯（目前是 `SUMMARY_WEEKDAY` 常數比對星期幾，先前的區間版本已被取代）。
27. **修正 `app.py` 的紅綠配色反邏輯**：呼應第 26 則結尾發現的問題，使用者確認要修。`app.py:24`（大盤漲跌）、`app.py:41`（個股損益）、`app.py:153`（總報酬）三處原本是「獲利/上漲=綠、虧損/下跌=紅」，改成台股慣例「紅漲綠跌」。用真實持股資料跑 `render_report_html()` 並用正規表達式擷取實際渲染結果驗證顏色跟漲跌方向對應正確（例如報酬 +14.6% 顯示紅色、-15.1% 顯示綠色）。
    - **How to apply**：以後任何新的網頁/報告如果要加損益顏色標示，記得台股慣例是紅漲綠跌，跟歐美股市（綠漲紅跌）相反，這個專案統一採台股慣例（太太的 `sector_rotation.py`／`rotation_tracking.py` 已經是這個慣例，這次是把 `app.py` 補齊到一致）。
28. **潛力新標的觀察（`candidate_watchlist.py`，新增）**：使用者發現「我的部分怎麼都沒有推薦的其他優質股票，都在我的持股裡運算」——`scoring.py`／`portfolio.py` 整套系統從頭到尾只分析 `個人持股明細.xlsx` 裡已經持有的股票，完全沒有「發掘目前沒持有的優質新標的」這塊功能，跟太太的 `sector_rotation.py`（有 `WATCHLIST` 可以掃）不對稱。
    - **動工前先問清楚範圍**（`AskUserQuestion`）：候選股池怎麼界定（人工整理清單 vs 特定指數成分股 vs 全市場掃描）、篩出的候選股要怎麼呈現。使用者選「綜合上述規則選5檔優質股，也可以是ETF」＋「獨立區塊列在每日報告」，接著在同一輪對話又補了兩次需求：先改成「6檔，3檔高價股3檔低價股」，這是最終版本。
    - **候選池刻意用人工整理清單，不是全市場掃描**：`CANDIDATE_POOL` 收錄約 25 檔台股常見權值股/優質股（涵蓋半導體/金融/電信/傳產/紡織等多元產業）+ 6 檔熱門 ETF，比照太太 `sector_rotation.py` 的 `WATCHLIST` 做法。上市櫃合計近 2000 檔，全市場逐檔套六構面評分（要抓財報/技術面/籌碼面）用免費資料源成本太高，不可行。
    - **關鍵設計決定：候選股不能套用 `decide_signal()`**——實作前用程式碼檢查發現，`decide_signal()` 的分支邏輯從 `h.pnl_pct` 開始（`if pnl_pct is None: signal, label = "🟡", "尚無市價資料"`），候選股沒有持有成本、`pnl_pct` 必為 `None`，直接套用會讓每一檔候選股都顯示「尚無市價資料」，完全沒有意義。改在 `scoring.py` 新增 `evaluate_candidate()`，共用 `score_fundamentals`/`score_growth`/`score_chips`/`score_technicals`/`score_valuation`/`score_conviction`/`score_risk` 這些子評分函式，算出 Investment Score／Risk Score／Conviction Score，但**不套用**任何「該不該賣」的持倉判斷——候選股只回答「值不值得列入觀察」。
    - **實作細節**：`candidate_watchlist.evaluate_candidates(existing_holdings, environment_score, top_n_each=3)`——排除目前已持有代號、排除現在追高（現價超過法人目標價20%以上，沿用既有 `CHASE_HIGH_THRESHOLD`）的候選股；`score_risk()` 的 `sector_weight` 刻意用「使用者現有持股」的產業佔比去查（不是候選股自己的），這樣如果候選股剛好落在使用者已經很重壓的產業，Risk Score 會正確反映「這會加重你的產業集中度」，是重用既有基礎架構的意外好處。用合成的 `Holding(shares=0, avg_cost=0)` 餵給既有的 `fetch_market_data()`/`build_market_intel()`/`build_technicals_map()`，全部沿用既有 pipeline，沒有另外寫一套資料抓取邏輯。
    - **高價股/低價股各3檔**：候選清單依股價排序切成兩半（中位數切分，不是固定金額門檻，隨候選池組成變動自動調整），各組內依 Investment Score 取前3名。
    - **全面串接**：`portfolio.build_report_context()` 新增 `candidate_watchlist` 欄位；`report.py`（第十節）／`excel_report.py`（新增「潛力新標的觀察」分頁）／`pdf_report.py`（第十節）／`app.py`（網頁儀表板新區塊）／`daily_report.py`（Email 摘要新增簡短清單）五處全部串好，跟既有持股的區塊分開列。
    - **效能**：候選股評分要對約20-25檔（扣掉已持有的）逐一抓財報/技術面/籌碼面，實測跑一次約35秒，會讓 `daily_report.py` 的總執行時間變長，但因為排程在 05:00 背景執行，使用者不會感覺到延遲。
    - **驗證方式**：先獨立測試 `evaluate_candidates()` 用真實持股+真實市場資料跑通（確認回傳6檔、高低價各3檔、分數合理）；再依序測 `report.py`（Markdown）／`excel_report.py`（讀回 Excel 分頁內容比對）／`pdf_report.py`（無崩潰）／`app.py`（擷取渲染出的 HTML 片段比對）／`daily_report.build_email_summary()`（比對文字輸出），最後跑一次真實的 `daily_report.py` 完整寄送 Email＋LINE 驗證整條 pipeline 沒有崩潰。
    - **How to apply**：候選池 `CANDIDATE_POOL` 是人工維護的清單，之後要增減候選股直接改這個 dict；高低價分組數量改 `evaluate_candidates()` 的 `top_n_each` 參數。
29. **個人短期目標追蹤（新增）**：使用者設定「目前股票總市值，明年此時要在股市賺到一個目前的總市值，且期間總市值不低於現在」。
    - **關鍵設計決定：獲利指標用「總市值−總成本」而非「總市值」**——因為使用者每月會透過定期定額持續投入新資金，如果直接拿「總市值」跟基準比較，新增的投入金額會虛增數字，變成「錢加越多看起來越像達標」，不是真的「賺到」。「總市值−總成本」（`ctx["total_pnl"]`）已經是現有系統既有的成本基礎獲利指標，新增投入的資金會同時墊高總市值和總成本，不會虛增這個數字，剛好是正確的追蹤指標，不需要另外設計。
    - **基準值是當下（2026-07-25）真實抓取的總市值，原本寫死在 `portfolio.py` 常數**（`GOAL_BASELINE_DATE`／`GOAL_TARGET_DATE`／`GOAL_BASELINE_VALUE`，比照 `MONTHLY_INVESTMENT` 的既有模式），不是每次執行都重新計算基準——基準本來就該固定在設定當下那一刻，之後總市值變動是用來跟這個固定基準比較，不是拿當下市值當基準會變成「永遠達標」的無意義比較。**後續調整**（見第 31 則）：`GOAL_BASELINE_VALUE` 這個真實金額改從 `.env` 讀取，不再寫死在程式碼／文件裡。
    - **`portfolio.goal_progress(total_value, total_pnl)`**：算出達成率（`total_pnl / GOAL_BASELINE_VALUE`）、是否跌破下限（`total_value < GOAL_BASELINE_VALUE`）、進度是否符合預期（依「已過天數／總天數」的比例回推「這個時間點應該要有多少累積獲利」，跟實際累積獲利比較，不是簡單看「還有沒有賺錢」）。
    - **全面串接，放在報告最前面**：`report.py`（新增在「一、投資組合總覽」之前，不佔用既有章節編號）／`pdf_report.py`（同樣放在最前面）／`excel_report.py`（新增「個人目標追蹤」分頁）／`app.py`（網頁儀表板最前面）／`daily_report.py`（Email 摘要總市值那行下面加一行）五處全部串好。PDF 版的 ✅/⚠️ 符號比照既有 `_pdf_safe()` 機制轉成【符合】/【注意】文字（KaiU 字型不支援 emoji，新增 `"✅": "【符合】"` 到 `_TEXT_REPLACEMENTS`）。
    - **驗證方式**：先用真實持股資料獨立測試 `goal_progress()` 確認數字合理（達成率為正值、剩餘天數正確、進度符合預期、未跌破下限）；再依序測 Markdown／Excel／PDF／app.py／Email 摘要五處輸出都正確顯示，過程中一次筆誤（`lines.extend([...]` 忘記補收尾的 `)`）被 IDE 診斷即時抓到並修正。
    - **How to apply**：這是使用者的一次性個人設定，之後如果要重新設定目標（例如明年到期後重新設定下一年度），直接改 `portfolio.py` 開頭的三個 `GOAL_*` 常數，不需要動 `goal_progress()` 的計算邏輯。
30. **重新整理 Claude Code 技能與技能清單文件**：本輪對話新增了大量功能（AI Email 上色/字級、每週五預測追蹤彙總、盤中交易時段守門、候選新標的觀察、個人目標追蹤），既有 6 個技能的 SKILL.md 內容跟著過時了，使用者要求「重新整理一遍技能和代理人」。
    - **抓到的過時內容**：`daily-report/SKILL.md` 還寫著舊排程時間「06:30」（已改 05:00）；`investment-report/SKILL.md` 還在講已經被六構面 AI 評分系統取代的舊門檻「停利+30%／停損-20%」（現制是 TAKE_PROFIT_THRESHOLD=+50%／STOP_LOSS_HARD_THRESHOLD=-30%，且訊號改用🟢🟡🟠🔴四色，不是單一門檻判斷），也沒提到新加的「個人短期目標追蹤」「潛力新標的觀察」兩個新章節；`sector-rotation-preview/SKILL.md` 還寫「前3強族群、每族群前2檔個股」跟舊的9組 WATCHLIST（現在是前5強、每族群前5檔、跨族群個股動能Top5、11組族群含新增的重電/軍工）。全部更新為目前實際行為。
    - **抓到一個結構性缺口**：`spouse_intraday_check.py`（09:35／11:30 排程）是唯一沒有對應技能的日常排程腳本——`daily_report.py`有`/daily-report`、`spouse_report.py`有`/spouse-report`，但盤中觀察沒有手動補寄的技能。新增 `/spouse-intraday-check`（haiku，機械式操作），並在說明裡特別強調「非交易時段執行只會印出略過訊息、不會寄信，這是設計行為不是錯誤」——避免使用者以為技能壞掉。
    - **技能清單.docx 改用程式產生，不再手動編輯 Word 檔**：新增 `generate_skills_doc.py`（新增 `python-docx` 套件依賴），讀取所有 `.claude/skills/*/SKILL.md` 的 frontmatter（name/model/description）自動組出文件，用完即棄的手動編輯方式容易漏改、跟實際技能內容脫節。之後任何技能異動，重跑這支腳本就能同步文件，不需要再手動排版 Word。
    - **`CLAUDE.md` 技能數量同步更新**：從「現有 6 個技能」改成「現有 7 個技能」，並列出新增的 `spouse-intraday-check`。
    - **驗證方式**：跑 `generate_skills_doc.py` 後用 `python-docx` 讀回 `技能清單.docx` 的段落內容，確認 7 個技能的名稱／model／描述都正確产生。
    - **How to apply**：以後新增技能的標準流程：(1) 在 `.claude/skills/<name>/SKILL.md` 寫好 frontmatter 與內容、依 `CLAUDE.md` 的複雜度規則指定 `model`；(2) 跑 `uv run python generate_skills_doc.py` 同步 `技能清單.docx`；(3) 更新 `CLAUDE.md` 的技能數量與清單。既有技能內容如果因為程式邏輯變動（門檻、排程時間、章節數）而過時，也要記得回頭更新對應的 SKILL.md，不是只改程式碼。
31. **同步專案到 GitHub（private repo）＋ 上傳前的隱私檢查**：使用者要求把本機專案同步到 GitHub，過程中主動做了幾層防護，不是收到指令就直接 push。
    - **`.gitignore` 審查**：使用者自己先手動加寬規則（`*.md`／`*.xlsx`／`*.json`），推送前用 `git add -A --dry-run` 逐一確認實際會被加入版本控制的檔案清單，確認 `.env`、`個人持股明細.xlsx`、`README.md`／`CLAUDE.md`／`專案進度.md`、`alert_state.json`／`rotation_predictions.json` 都正確被排除。
    - **抓到一個寫死在程式碼裡的真實淨資產數字**：`portfolio.py` 的 `GOAL_BASELINE_VALUE`（第29則設定的個人目標基準）是實際抓取的真實總市值，寫死在程式碼裡的話會被永久留在 git 歷史紀錄，即使之後刪掉也清不掉。**修正**：改成 `float(os.getenv("GOAL_BASELINE_VALUE", "0"))` 從 `.env` 讀取，`.env`（含真實金額）與 `.env.example`（空白欄位範本）分別更新，`portfolio.py` 補上 `load_dotenv()`。
    - **意外發現 README.md 被損毀**：使用者疑似依照 GitHub 網頁建立空 repo 後顯示的制式指令操作（`echo "# 專案名" >> README.md`），但在 Windows PowerShell 底下 `>>` 預設輸出 UTF-16LE 編碼，把這行文字以 UTF-16 附加到原本 UTF-8 的 README.md 尾端，產生一段夾雜 `\x00` 空位元組的亂碼片段。用 Python 直接操作 bytes 找到 `\x00` 位置、截掉損毀片段、還原成乾淨 UTF-8 修復。**How to apply**：以後如果有人回報「這個文字檔開起來後面一段亂碼」，先懷疑是不是被不同編碼的內容意外附加到尾端，用 `data.find(b'\x00')` 這類方式定位比逐字檢查快很多。
    - **本機已完成 `git init`（分支 `main`）、初始 commit（31個檔案）**；`git push` 被 Claude Code 自動模式的權限分類器擋下（推送到遠端屬於需要使用者明確授權的敏感動作），改請使用者自行執行或下次提示時授權。
    - **嘗試把資料夾從「Stock investment」改名成「Stock_investment」失敗且刻意放棄**：用 `mv` 改名時遇到「Device or resource busy」——因為所有指令的工作目錄都被工具自動鎖定在這個資料夾本身，等於要對自己站著的地板動手。就算繞過限制改名成功，工具後續每次執行都還是會被自動導回舊路徑（該路徑已不存在），**會讓當次對話後續完全無法對這個專案做任何操作**。判斷風險後主動停手，改建議使用者自己用檔案總管手動改名、改完重開 Claude Code 指向新路徑，不冒險用工具硬做。
    - **使用者主動要求檢查 README.md／CLAUDE.md／專案進度.md 三份文件的個資／密碼／帳號**：逐一 grep 真實 email、凱基密碼、LINE token、真實金額等關鍵字。CLAUDE.md 乾淨；README.md 只有 1 處真實總市值數字（「個人短期目標追蹤」段落）；**專案進度.md 問題最大**——整份文件在記錄「已用真實資料驗證」的過程中，反覆引用了使用者真實持有的個股名稱與代號、真實損益率、真實法人目標價、真實月配置金額、真實 Conviction Score 分數，分散在十幾處條目裡。使用者選擇「全部改成通用範例」，逐條把真實股票名稱代號、真實百分比、真實金額改成「某檔獲利部位」「某檔虧損超過-30%硬門檻的持股」之類的通用敘述，保留技術邏輯與驗證方法論的完整性，只拿掉會洩漏真實持股組成與績效的具體數字。**寫這則紀錄本身也要小心同一個坑**：一開始起草這段說明時，為了交代清楚改了什麼，直接把原本的真實股票名稱與數字又寫回這則新增的條目裡，等於做了等於沒做，後來自己重新檢查才發現並改寫成純敘述、不重複具體數字。
    - **刻意沒有改動的部分**：太太短線輪動系統（`sector_rotation.py`／`WATCHLIST`）裡提到的真實股票範例（萬海／和碩／聯詠／技嘉／廣達／長榮／智邦／重電軍工新增股等）維持原樣不做通用化——這些是公開市場的熱門股代表清單，不是使用者自己的持股組成，測試時的即時報酬率只是驗證掃描機制正常運作，不構成個資揭露。
    - **How to apply**：以後任何要記錄進 `專案進度.md` 的「已用真實資料驗證」內容，如果驗證對象是使用者自己的持股／損益／目標價／資產總額，寫的時候就該直接用「某檔獲利部位」「某檔虧損超過X%門檻」這種通用敘述法，不要圖方便直接寫真實股票代號與數字——這份文件的定位已經確定是要準備上傳到（即使是 private）GitHub repo，不是只留在本機的私人筆記。
32. **`MONTHLY_INVESTMENT`（每月定期定額投入金額）比照 `GOAL_BASELINE_VALUE` 改為 `.env` 讀取**：使用者發現 `專案進度.md` 裡直接寫出每月定期定額的實際金額，屬於個人財務資料，要求修改；順手也把同一個數字在 `portfolio.py` 裡寫死的問題一併處理（該常數本來就已 commit 進 git 歷史）。
    - `專案進度.md` 內三處明確寫出金額數字的條目（v1.0 設定說明、薪資現金流投資人系統定義、分批投入 bug 修復紀錄）都改成不揭露具體數字的通用敘述。
    - `portfolio.py` 的 `MONTHLY_INVESTMENT` 從寫死的常數改成 `float(os.getenv("MONTHLY_INVESTMENT", "0"))`，做法與注釋比照既有的 `GOAL_BASELINE_VALUE`；`.env`（含真實金額）與 `.env.example`（空白欄位範本）都同步新增對應設定項，`.env` 本身已 gitignore。
    - 已用 `uv run python -c "import portfolio; print(portfolio.MONTHLY_INVESTMENT)"` 驗證能正確從 `.env` 讀回數值（型別從 `int` 變成 `float`，程式內唯一使用處 `amount: float = MONTHLY_INVESTMENT` 本來就是 `float` 型別，不受影響）。

| 檔案 | 用途 |
|---|---|
| `個人持股明細.xlsx` | 原始持股資料（代號、名稱、股數、平均成本）＋「現金流」分頁（日期/項目/金額/備註，供薪資現金流投資人系統累計現金池） |
| `fill_names.py` | 依代號填入股票名稱的一次性工具腳本 |
| `portfolio.py` | 核心分析邏輯（抓價、基本面、風險、再平衡、每月建議） |
| `report.py` | 產生 Markdown 版投資報告（`投資報告.md`） |
| `excel_report.py` | 產生 Excel 版投資報告（`投資報告.xlsx`） |
| `main.py` | 命令列執行入口，同步輸出 Markdown 與 Excel |
| `app.py` | FastAPI 網頁儀表板 |
| `kgi_client.py` | 凱基 API 連線包裝（報價、庫存查詢；不含下單） |
| `test_kgi_connection.py` | 凱基 API 連線測試腳本（需使用者本機執行） |
| `.env.example` | 凱基 API 帳密設定範本（無真實帳密） |
| `market_data.py` | v4.0 月營收、財報、重大訊息、新聞、大盤指數、三大法人買賣超、融資融券、董監/大股東持股、資產負債表、營益分析、股利分派（免費公開資料源） |
| `technicals.py` | 技術面指標（均線/RSI/KD/MACD/布林/ATR/支撐壓力/量能），yfinance 歷史股價自算 |
| `market_environment.py` | 市場環境（VIX/費半/那斯達克/美元/台幣/TSM ADR/NVDA/AMD/INTC） |
| `scoring.py` | AI 綜合評分引擎：六構面加權評分 + Risk Score + 四色訊號決策 + 節流狀態記憶 + 候選新標的評估 |
| `candidate_watchlist.py` | 潛力新標的觀察：人工整理權值股/優質股/ETF候選池，篩出未持有、評分最高的高價股/低價股各3檔 |
| `rotation_tracking.py` | 太太短線輪動預測的事後驗證追蹤（記錄預估報酬率、天數到期後比對落差並推斷原因，每週五彙總寄給使用者） |
| `pdf_report.py` | 產生 PDF 版投資報告（`投資報告.pdf`，用 reportlab 直接排版，標楷體、大字體） |
| `notify.py` | v5.0 Email 通知（SMTP）＋ LINE 通知（Messaging API Broadcast） |
| `daily_report.py` | v5.0 每日排程入口（產生報告＋寄 Email／LINE，套用 AI 評分節流邏輯） |
| `sector_rotation.py` | 短線類股輪動掃描（3-7天動能排名、均線買賣區間、預估天數、潛在損益率） |
| `spouse_report.py` | 短線輪動觀察獨立寄給太太（BCC給使用者），已排入05:00排程 |
| `microstructure.py` | 盤中開盤強弱評分（開盤溢價率/委買委賣量/量能確認），僅供太太的短線輪動觀察使用 |
| `spouse_intraday_check.py` | 盤中警示（09:35／11:30 排程），寄給太太的盤中強弱更新 |
| `.claude/skills/` | 7 個 Claude Code 專案技能（觸發詞與模型分級整理在 `技能清單.docx`） |
| `generate_skills_doc.py` | 讀取 `.claude/skills/*/SKILL.md` 自動重新產生 `技能清單.docx` |
| `CLAUDE.md` | 專案層級規範：技能 model 分級規則、AI_Agent 架構參考對照表 |
| `AI_Agent_架構規格_v2.0.md` | 使用者提供的理想化 21-Agent 架構文件，僅當概念參考，不拆成真的 subagent |
| `README.md` | 專案說明與使用方式 |

## 尚待補充 / 需要使用者提供的資源

- **券商 API（v3.0）**：API 申請已完成，程式碼已備妥。深入排查後確認卡在 `CGEnvDetectATL`／`KGICGCAPIATL2` 這組 2014 年舊元件與現行後端協定疑似不相容（access violation），已排除 Python 版本、憑證有效性、防火牆／防毒問題。**需要凱基技術支援提供目前版本相容的元件，才能繼續**。
- **上櫃股（TPEx）月營收／財報**：TPEx 開放資料網站 SSL 憑證設定有問題，程式端連線失敗；本專案目前持股皆為上市公司，暫無影響。

## 未來版本規劃（摘自規劃書）

- v2.0：自動匯入 Excel
- v3.0：串接凱基證券 API（卡關中，見上）
- v4.0：整合新聞、法說會、月營收、財報（**全部完成**：月營收／財報／重大訊息／新聞／法說會排程）
- v5.0：每日自動產生投資報告與提醒（**Email 通知＋LINE 通知＋每日排程（06:30）全部完成並驗證成功**）
